# Carelane workplan — 2026-05-30

This is the consolidated todo list for the current release cycle.

Legend:
- `[x]` done
- `[ ]` not done yet
- `[!]` blocked on external production input

## Done now

- [x] Fixed the PostgreSQL `FOR UPDATE` lock issue in provider / placement / intake flows.
- [x] Validated the 3 failing PostgreSQL workflow tests.
- [x] Re-ran `./scripts/production_readiness_gates.sh` successfully on local Postgres.
- [x] Re-ran `./scripts/go_live_django_preflight.sh` successfully with `check --deploy` clean.
- [x] Re-ran `./scripts/production_go_live_preflight.sh` successfully with the release preflight flow.
- [x] Added rollout execution templates for production handoff.
- [x] Added drill log production evidence template.
- [x] Reran the provider Playwright smoke slice and the golden path rehearsal successfully after removing networkidle login waits.
- [x] Seeded an active pending-placement case so the provider Playwright smoke coverage now runs instead of skipping.
- [x] Added the Render deploy hook documentation and manual trigger path.
- [x] Added backup / restore drill worksheet.
- [x] Added observability / alerting notes sheet.
- [x] Polished provider review intro copy for the handoff / advisory context.
- [x] Cleaned up visible operationele-coördinatie terminology in the shell copy.

## Must

- [!] Schedule the production window with a date and time slot.
- [!] Confirm the release captain, backend owner, ops owner, and QA owner.
- [!] Confirm rollback owner and rollback path.
- [!] Confirm production backup exists and is recent.
- [!] Confirm production secrets and environment readiness.
- [!] Rotate overdue `OIDC_RP_CLIENT_SECRET` before real production use.
- [!] Run production preflight against the real production environment.
- [!] Deploy release SHA `ca146bdc` to production.
- [!] Run `migrate --noinput` on production.
- [!] Run `collectstatic --noinput` on production.
- [!] Restart the production service.
- [!] Run `check --deploy` on production.
- [!] Run terminology guard on production.
- [!] Run production smoke checks for dashboard and canonical `/care/` route.
- [!] Start and end the monitoring watch window.
- [!] Record sign-off and archive evidence in `docs/DRILL_LOG.md`.

## Should

- [ ] Record the final production timestamps in `docs/RELEASE_EXECUTION_SHEET_2026-05-30.md`.
- [ ] Copy the final rollout evidence into `docs/DRILL_LOG.md`.
- [ ] Archive `check --deploy` output and smoke-test output.

## Could

- [x] Review whether the structural `regiekamer` rename should stay deferred.

## Not now

- [ ] Any autonomous AI feature work outside the current advisory scope.
- [ ] Structural rename / `regiekamer` key rename until provider-path stability is done. See `docs/STRUCTURAL_RENAME_DEFERRED.md`.

## Immediate next action

1. Choose the production window.
2. Confirm owners.
3. Rotate / confirm secrets.
4. Run production preflight and deploy when the window opens.
5. After production, copy the actual timestamps and evidence bundle into the execution sheet and drill log.

## Compact production checklist

- [ ] Confirm production window and owners.
- [ ] Confirm rollback owner/path.
- [ ] Confirm production backup is recent.
- [ ] Confirm secrets/env readiness.
- [ ] Rotate `OIDC_RP_CLIENT_SECRET`.
- [ ] Run production preflight on the real environment.
- [ ] Deploy `ca146bdc`.
- [ ] Run `migrate --noinput`.
- [ ] Run `collectstatic --noinput`.
- [ ] Restart the service.
- [ ] Run `check --deploy`.
- [ ] Run terminology guard.
- [ ] Run smoke checks for dashboard and `/care/`.
- [ ] Start and end monitoring watch.
- [ ] Record sign-off and archive evidence in `docs/DRILL_LOG.md`.
