export const CHART_COLORS = {
  /** Axis lines, tick marks, and axis labels. */
  axis: "#9CA3AF",
  /** Grid lines and tick strokes. */
  grid: "rgba(167, 139, 250, 0.10)",
  /** Primary data series line/bar. */
  line: "#8B5CF6",
  /** Secondary label emphasis (e.g. bold category names). */
  labelBold: "#E7E7F0",
  /** Box-shadow for chart card borders and glow. */
  cardGlow: "0 0 0 1px rgba(168,85,247,0.25), 0 0 28px rgba(168,85,247,0.10)",
} as const;
